{
  "name": "guardian-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Guardian defensive agent wallet — GuardVault + mandate verification (ETHOnline 2026)",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.29.1"
  },
  "dependencies": {
    "@bgd-labs/aave-address-book": "^4.44.22",
    "@openzeppelin/contracts": "^5.6.1"
  }
}

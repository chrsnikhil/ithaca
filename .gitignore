# ─── dependencies ───
**/node_modules/

# ─── SECRETS — never commit (templates .env.example / .env.local.example are kept) ───
**/.env
**/.env.*
!**/.env.example
!**/.env.local.example
**/.arm-mandate.json
**/policy.json
**/policy-multi.json

# ─── build / derived ───
**/.next/
**/dist/
**/build/
**/out/
**/.vercel/
next-env.d.ts
*.tsbuildinfo

# ─── demo deck: heavy / derived artifacts (keep index.html + assets) ───
guardian-demo/.shots/
guardian-demo/frames/
guardian-demo/*.mp4
guardian-demo/**/*.tmp.json

# ─── large / generated / backups (not part of the submission) ───
generated/
avatars_24src_backup/
Itinerary.pdf

# ─── OS / editor ───
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# stray tool binaries
*.exe
cloudflared*

# ─── root scratch: planning docs & reference art (kept locally, not in the repo) ───
/BUILD_PLAN.md
/SPONSOR_PAINPOINT.md
/STYLE_GUIDE.md
/style-preview.html
/images.jpeg
/char*.png

# ─── heavy media (demo video is a hosted link, not in the repo) ───
*.mp4
*.mp3
*.docx
